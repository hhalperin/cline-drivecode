/**
 * @deprecated Import from `./visualEngine` — kept as a thin alias so older
 * call sites and docs that name `diagramPresentation` keep resolving.
 */
export {
	DIAGRAM_NARROW_MAX_PX,
	DIAGRAM_WIDE_MIN_PX,
	type DiagramLayout,
	type DiagramSurface,
	diagramLayoutForViewport,
	mermaidFontSizeForViewport,
	shouldAutoRenderMermaid,
} from "./visualEngine";
