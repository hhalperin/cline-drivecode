{
	"name": "@kanban/desktop-tauri",
	"version": "0.0.1",
	"private": true,
	"description": "Tauri host adapter for Kanban's desktop bridge — the only place @tauri-apps/api is imported",
	"type": "module",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"@kanban/desktop-bridge": "workspace:*",
		"@tauri-apps/api": "^2.11.1"
	},
	"devDependencies": {
		"typescript": "^5.9.3",
		"vitest": "^4.1.0"
	}
}
