---
title: "SDK integration build"
sidebarTitle: "Integration build"
description: "Visual + actionable plan to close public @cline/sdk leverage gaps across product hosts."
---

# SDK integration build

**Public SDK:** [docs.cline.bot/sdk/overview](https://docs.cline.bot/sdk/overview) — `npm install @cline/sdk` (Node ≥22). Same harness as Cline IDE/CLI: plugins, checkpoints, MCP, cron, subagents, and more.

**Visual plan (primary):** Tldraw canvas `giaumqy8` — package map, leverage heatmap, decision diamonds D1–D4, phase strip, agent swimlanes, kickoff order.

**Companion detail:** task acceptance criteria below (and [Usage analysis](/sdk/architecture/usage-analysis)).

**Monorepo SDK version policy:** Product hosts and examples resolve `@cline/*` via `workspace:*` (always the packages under `sdk/packages/`). Published npm latest for `@cline/sdk` is currently **0.0.66**, matching workspace `@cline/{sdk,core,agents,llms,shared,drive}`. Do not pin numeric `@cline/*` versions inside this monorepo. Keep package `version` fields in lockstep via `sdk/scripts/version.ts` when cutting releases.

Turns usage analysis into a sequenced build against the **advertised public SDK**. Phases are ordered by dependency — **no calendar estimates**. Each **agent task** is one focused PR.

**Source of truth for symbols:** code (`DefaultToolNames`, `TEAM_TOOL_NAMES`, `spawn_agent`, `ClineCore` APIs) + [public docs](https://docs.cline.bot/sdk/overview).

## North star

Product hosts stay on **`ClineCore`**. Success means:

1. Hosts share **one defaults story** for multi-agent, plugins-on-session, budgets, and Hub runtime depth.
2. Public docs surfaces either **match product paths** or docs name the product-canonical alternative (esp. scheduling vs `cline.automation`).
3. An agent can pick any open task and land it without inventing product policy.

## Non-goals (whole build)

- Replacing host adapters (VS Code terminal/diff/`McpHub`, Hub WS protocol) with a thin SDK client.
- Adopting `backendMode: "remote"` or browser automation without a separate product decision.
- Rewriting evals to construct SDK objects (CLI/Harbor behavioral coverage is enough).

## Decision gates — LOCKED

| ID | Decision | **Locked** choice | Unlocks |
|----|----------|-------------------|---------|
| **D1** | Multi-agent host defaults | **CLI non-YOLO semantics** as product default: `enableSpawnAgent: true`, `enableAgentTeams: true` unless YOLO/sandbox/ACP special-cases. Hub Chat + Desktop adopt the same defaults. VS Code: expose a setting (default **on** once UI is honest); until then do not leave dead subagent UI. | Phase 2 |
| **D2** | Scheduling canonical path | **Hub `schedule.*` / CLI / Desktop routines** are product-canonical. Public scheduled-agents docs lead with that path; `cline.automation` is the embedder/SDK-local alternative. | Phase 3 |
| **D3** | Plugin session injection | Installed/enabled plugins load into `CoreSessionConfig` via `pluginPaths` / `extensions` + `extensionContext.workspace` on session start. | Phase 4 |
| **D4** | Cost budgets | Shared `maxIterations` defaults (+ optional usage abort helper); hosts may override via settings/UI. | Phase 5 |

Amend only by editing this table (and the visual plan). Agents must not invent alternate policy.

## Architecture note — MCP split brain (SDK-0.3)

| Host | MCP path |
|------|----------|
| CLI / Hub / Desktop | Core MCP settings file + install/OAuth helpers; settings tools available unless disabled |
| VS Code | `disableMcpSettingsTools: true`; tools injected via legacy `McpHub` + `createMcpTools` |

This is intentional adapter debt, not a one-line flip. Do not enable VS Code settings MCP tools without a migration spike (`SDK-7.2`).

## Visual structure

```txt
[@cline/sdk @ 0.0.66 workspace:*]
        ↓
[North star | Non-goals]
        ↓
[Feature leverage heatmap]
        ↓
[D1–D4 LOCKED]
        ↓
[P0 → P1 → … → P7]
        ↓
[Swimlanes + kickoff order]
```

## Phase overview

| Phase | Goal | Gate |
|-------|------|------|
| **0** | Decisions locked; skill/docs symbol drift fixed; MCP note; version lockstep | D1–D4 locked; wire names match Core |
| **1** | Shared helpers for session features + plugin load | Unit tests in `@cline/core` |
| **2** | Multi-agent flags consistent across hosts | Defaults match D1 |
| **3** | Scheduling docs match product | Product path first in docs |
| **4** | Plugins participate in agent loop | `pluginPaths`/`extensions` on start |
| **5** | Iteration budgets + optional usage abort | Defaults applied; abort tested |
| **6** | Hub `ClineCore.create` telemetry/compaction/hooks | Matches CLI depth |
| **7** | VS Code hooks + MCP honesty | Wired or explicit wontfix |
| **8** | OTEL / event projection (optional) | Incremental adoption |

---

## Phase 0 · Truth and decisions

### Agent task `SDK-0.1` — Lock decisions D1–D4

| | |
|---|---|
| **Status** | **Done** — table above marked LOCKED |
| **Done when** | Each decision has a single chosen lock |

### Agent task `SDK-0.2` — Fix skill/docs symbol drift

| | |
|---|---|
| **Status** | **Done** (this PR) — skills + `docs/sdk/guides/multi-agent-teams.mdx` aligned to `DefaultToolNames` / `spawn_agent` / `TEAM_TOOL_NAMES` |
| **Note** | `start_subagent` remains documented only as **agents-squad plugin** tools, not Core builtins |

### Agent task `SDK-0.3` — MCP split-brain note

| | |
|---|---|
| **Status** | **Done** — section above + architecture overview see-also |

---

## Phase 1 · Shared session defaults helper

### Agent task `SDK-1.1` — Extract `resolveProductSessionFeatures`

| | |
|---|---|
| **Depends on** | D1, D4 (locked) |
| **Owner surface** | `@cline/core` |
| **Goal** | Pure helper → `{ enableSpawnAgent, enableAgentTeams, maxIterations?, ... }` from host context (`yolo`, `acp`, overrides). |
| **Verify** | `bun run build:sdk` then focused `bun -F @cline/core test:unit`. |
| **Status** | **Done** — `resolveProductSessionFeatures` + tests in `@cline/core` (hosts wire in Phase 2/5) |

### Agent task `SDK-1.2` — Extract `resolveSessionPluginLoad`

| | |
|---|---|
| **Depends on** | D3 (locked) |
| **Goal** | `{ pluginPaths, extensions, extensionContext }` for `CoreSessionConfig`. |
| **Verify** | `bun run build:sdk`; fixture plugin unit test. |
| **Status** | **Done** — `resolveSessionPluginPaths` / `buildSessionExtensionWorkspace` / `resolveSessionPluginLoad` + tests in `@cline/core` (hosts inject in Phase 4) |

---

## Phase 2 · Multi-agent alignment

`SDK-2.1` CLI · `SDK-2.2` Hub · `SDK-2.3` Desktop · `SDK-2.4` VS Code — wire helper / D1 defaults.

| | |
|---|---|
| **Status** | **Done** — hosts call `resolveProductSessionFeatures` (CLI + ACP, Hub server + Chat defaults, Desktop sidecar, VS Code session factory) |

## Phase 3 · Scheduling story

### Agent task `SDK-3.1` — Align public scheduled-agents guide

| | |
|---|---|
| **Depends on** | D2 (locked) |
| **Owner surface** | `docs/sdk/guides/scheduled-agents.mdx` + `.agents/skills/cline-sdk` scheduling refs |
| **Goal** | Public guide and skill lead with product-canonical Hub `schedule.*` / CLI / Desktop path; document `cline.automation` as the embedder/SDK-local alternative (file specs + `reconcileNow` / ingest / list*). Fix API drift (`reconcile` → `reconcileNow`). |
| **Verify** | Ripgrep: `HubSessionClient` schedule methods, `ClineCoreAutomationApi`, `HubScheduleCreateInput`, Hub command names match docs. |
| **Status** | **Done** — guide + skill + `clinecore/api.md` automation snippet aligned to D2 |

### Agent task `SDK-3.2` — Optional automation bridge

| | |
|---|---|
| **Depends on** | D2 (locked), SDK-3.1 |
| **Goal** | Optional code bridge between Hub `schedule.*` and `cline.automation` / file cron (only if product wants one store). |
| **Status** | **Deferred** — plan has no acceptance criteria; D2 forbids replacing Hub schedules with automation. Hub already optionally runs a parallel `CronService` via `cronOptions` without merging stores. Revisit only with an explicit product decision + scoped design. |

## Phase 4 · Plugins on session

`SDK-4.1` CLI · `SDK-4.2` Hub+Desktop · `SDK-4.3` VS Code.

## Phase 5 · Budgets

`SDK-5.1` Shared `maxIterations` · `SDK-5.2` Optional usage abort.

## Phase 6 · Hub ClineCore depth

`SDK-6.1` Telemetry · `SDK-6.2` Compaction · `SDK-6.3` Hooks.

## Phase 7–8 · VS Code honesty + optional observability

`SDK-7.1` Hooks · `SDK-7.2` MCP spike · `SDK-8.1` OTEL · `SDK-8.2` Event projection.

---

## Per-task agent checklist

```text
You are executing task <SDK-X.Y> from docs/sdk/architecture/integration-build.mdx
(+ visual plan canvas giaumqy8).
Public API: https://docs.cline.bot/sdk/overview
Monorepo: use workspace:* @cline/* at sdk package version 0.0.66 (lockstep).
1. Respect LOCKED D1–D4.
2. One task only. No drive-by refactors.
3. After @cline/* edits: bun run build:sdk before host tests.
4. Verify per task. One task per PR when possible.
```

## Progress tracker

| Task | Status |
|------|--------|
| SDK-0.1 Decisions | **Done** |
| SDK-0.2 Skill/docs drift | **Done** |
| SDK-0.3 MCP split brain note | **Done** |
| SDK version lockstep (`@cline/drive` → 0.0.66) | **Done** |
| SDK-1.1 Product session features helper | **Done** |
| SDK-1.2 Session plugin load helper | **Done** |
| SDK-2.1–2.4 Multi-agent hosts | **Done** |
| SDK-3.1 Align scheduled-agents guide (D2) | **Done** |
| SDK-3.2 Optional automation bridge | **Deferred** (no merge of Hub schedules ↔ automation) |
| SDK-4.1–4.3 Plugins on session | Open |
| SDK-5.1–5.2 Budgets | Open |
| SDK-6.1–6.3 Hub depth | Open |
| SDK-7.1–7.2 VS Code hooks/MCP | Open |
| SDK-8.1–8.2 Observability | Open |

## Related

- [Usage analysis](/sdk/architecture/usage-analysis)
- [Public SDK overview](https://docs.cline.bot/sdk/overview)
- [ClineCore](https://docs.cline.bot/sdk/clinecore)
- [Multi-agent teams](https://docs.cline.bot/sdk/guides/multi-agent-teams)
- [Scheduled agents](https://docs.cline.bot/sdk/guides/scheduled-agents)
- [Going to production](https://docs.cline.bot/sdk/guides/going-to-production)
